
imports:
  - module: ./test/load/secrets/secretSource
    source: secretSource 

sources:
  - secretSource: {
      type: secretSource,
      secret-parameter: !secrets key1,
      public-parameter: publicValue
  }
